# Chat-shell design stubs

Static Obsidian light/dark HTML + CSS tokens for the P-1 chat-shell foundation.

- `docs/design/chat-shell-obsidian-*.html` — source of truth
- `public/design/` — same files for local static browsing via Vite
- Reference mock: three-column Scripts + creatives shell (brand sidebar, thread, images rail).

These are view-only stubs. Do not treat them as the production UI.
